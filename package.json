{
  "name": "aframe-hello-world-boilerplate",
  "description": "Boilerplate with A-Frame's 'Hello, World!' of WebVR.",
  "version": "0.3.0",
  "license": "MIT",
  "scripts": {
    "Originstart": "budo --live --verbose --port 3000 --open --ssl --cert=localhost.pem --key=localhost-key.pem",
    "start": "budo --live --verbose --ssl --cert=localhost.pem --key=localhost-key.pem",
    "https": "budo -S --cert=localhost.pem --key=localhost-key.pem",
    "deploy": "ghpages",
    "ghpages": "ghpages"
  },
  "devDependencies": {
    "budo": "^11.0.0",
    "ghpages": "0.0.10"
  },
  "keywords": [
    "aframe",
    "aframe-example",
    "aframe-boilerplate",
    "aframe-scene",
    "webvr",
    "vr"
  ]
}
